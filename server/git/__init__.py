"""Git integration package.

Import from the submodules directly (``server.git.core``, ``server.git.diff``,
``server.git.executor``, ``server.git.router``, ...); this package deliberately
has no re-export facade.
"""
