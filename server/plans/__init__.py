"""Plan documents: detailed plans the assistant saves via the create_plan tool.

Stored as gitignored markdown under data/plans/ so the conversation carries only
a short summary + a card, and the full plan opens in the right-side dock.
"""
